# Environment files (may contain secrets) — keep .env.example tracked
.env
.env.local
.env.*.local

# Submitted lead storage (contains PII) — keep the dir, ignore the data
/storage/*
!/storage/.gitkeep
